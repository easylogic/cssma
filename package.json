{
  "name": "monorepo-root",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter barocss-docs docs:dev",
    "build": "pnpm --filter barocss-docs docs:build",
    "serve": "pnpm --filter barocss-docs docs:serve",
    "preview": "pnpm --filter barocss-docs docs:preview",
    "release": "pnpm --filter barocss changeset version && pnpm --filter barocss changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "esbuild": "^0.25.9",
    "typescript": "^5.9.2"
  }
}