// Use a simple CommonJS file to debug
// Since we can't require TypeScript directly, let's create a simple test

console.log('Testing bg-cover parsing...');

// Let's check if the test can at least run
console.log('Test setup complete');
